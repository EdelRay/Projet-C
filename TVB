float TVB(char* bureau_vote.txt , int T[100][100] , int n)
{ int i,j,nb=0,ide,trouve;
float pour;
printf("donner votre identifiant\n");
scanf("%d",&ide);
i=0;
while ((i<n )&& (trouve=0))
{
	if (t[i]=!ide)
	   i++;
	else trouve=1;
	indi=i;
}
if (trouve==1)
{
	printf("choisir le numero de la liste\n");
	sacnf("%d",&c);
	t[indi][1] =c
}
for(i=0;i<n;i++){
  for(j=0;j<n;j++)
  {
     if t[i][j]==" ";
	nb=nb+1;
  }
}
pour=(nb/n)*100;
return pour;
